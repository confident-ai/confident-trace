name: Python
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [minimum, latest]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install -e './python[test]' build ruff
      - if: matrix.dependencies == 'minimum'
        run: >-
          python -m pip install
          opentelemetry-api==1.39.0 opentelemetry-sdk==1.39.0
          opentelemetry-exporter-otlp-proto-http==1.39.0
          opentelemetry-exporter-otlp-proto-grpc==1.39.0
          openai==1.109.0 anthropic==0.69.0 google-genai==1.40.0 wrapt==1.17.3 boto3==1.40.0 botocore==1.40.0
      - run: python tools/generate_genai.py --check --base "$REGISTRY_BASE"
        env:
          REGISTRY_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
      - run: ruff check python tools
      - run: ruff format --check python tools
      - run: pytest python/tests
      - run: cmp LICENSE python/LICENSE
      - run: python -m build python

  native:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [tested, latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - if: matrix.dependencies == 'tested'
        run: python -m pip install -c python/tests/constraints/native.txt -e './python[test,native-test]'
      - if: matrix.dependencies == 'latest'
        run: python -m pip install --upgrade -e './python[test,native-test]'
      - run: python -m pip check
      - run: pytest python/tests

  microsoft:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [tested, latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - if: matrix.dependencies == 'tested'
        run: python -m pip install -c python/tests/constraints/microsoft.txt -e './python[test,microsoft-test]'
      - if: matrix.dependencies == 'latest'
        run: python -m pip install --upgrade -e './python[test,microsoft-test]'
      - run: python -m pip check
      - run: pytest python/tests

  agent-sdks:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [tested, latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - if: matrix.dependencies == 'tested'
        run: python -m pip install -c python/tests/constraints/agent-sdks.txt -e './python[test,agent-sdks-test]'
      - if: matrix.dependencies == 'latest'
        run: python -m pip install --upgrade -e './python[test,agent-sdks-test]'
      - run: python -m pip check
      - run: pytest python/tests

  langchain:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [tested, latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - if: matrix.dependencies == 'tested'
        run: python -m pip install -c python/tests/constraints/langchain.txt -e './python[test,langchain-test]'
      - if: matrix.dependencies == 'latest'
        run: python -m pip install --upgrade -e './python[test,langchain-test]'
      - run: python -m pip check
      - run: pytest python/tests

  crewai:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        dependencies: [tested, latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - if: matrix.dependencies == 'tested'
        run: python -m pip install -c python/tests/constraints/crewai.txt -e './python[test,crewai-test]'
      - if: matrix.dependencies == 'latest'
        run: python -m pip install --upgrade -e './python[test,crewai-test]'
      - run: python -m pip check
      - run: pytest python/tests
